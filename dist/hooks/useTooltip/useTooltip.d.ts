import { ReactNode } from "react";
import { TooltipOptions, TooltipRefs } from "./types";
declare const useTooltip: (content: ReactNode, options: TooltipOptions) => TooltipRefs;
export default useTooltip;
