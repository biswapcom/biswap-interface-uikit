import styled from "styled-components";

// components
import { Box } from "../Box";

const Placeholder = styled(Box)`
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
`;

export default Placeholder;
